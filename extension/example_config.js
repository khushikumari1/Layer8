
const config = {
  API_URL: "",
  
  defaults: {
    enabled: true,
    debugMode: false
  },
  
  platforms: {
    CHATGPT: "chatgpt.com",
    GEMINI: "gemini.google.com",
    GROK: "grok.com"
  }
};

Object.freeze(config);